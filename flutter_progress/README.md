# Flutter Circular Progress Bar

This example demonstrates a simple circular progress bar that fills based on a
value you enter. Type a number between `0` and `100` in the text field and the
circle will fill to that percentage. The number inside the circle always shows
the current percentage.

```bash
flutter run lib/main.dart
```
